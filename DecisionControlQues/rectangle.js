var length = 5, breadth = 4;
var area = length * breadth;
var perimeter = 2 * (length + breadth);

if (area > perimeter) {
    console.log("Area is greater than Perimeter");
} else {
    console.log("Area is not greater than Perimeter");
}
